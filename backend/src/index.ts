// Absorbi backend package.
// Later phases add the Supabase client, the transparent scoring engine,
// and the shared input and output types (the contract) here.
export const ABSORBI_BACKEND_VERSION = "0.0.0";
